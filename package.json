{
  "name": "football-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "base-64": "^0.1.0",
    "isomorphic-fetch": "^2.2.1",
    "localStorage": "^1.0.3",
    "node-sass-chokidar": "^0.0.3",
    "react": "^15.6.1",
    "react-dom": "^15.6.1"
  },
  "devDependencies": {
    "react-scripts": "1.0.7"
  },
  "scripts": {
    "build-css": "node-sass-chokidar src/App.scss -o src/",
    "watch-css": "yarn build-css && node-sass-chokidar src/App.scss -o src/ --watch --recursive",
    "analyze": "source-map-explorer build/static/js/main.*",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "surge-deploy-prod": "HTTPS=true NODE_ENV=production yarn build && cp build/index.html build/200.html && surge --domain https://footballapp.onstash.me build/",
    "surge-deploy-test": "TEST=true HTTPS=true NODE_ENV=production yarn build && cp build/index.html build/200.html && surge --domain https://footballapp-staging.onstash.me build/",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
